# @package _global_
# HyperSteer experiment - only overrides what's different from base

defaults:
  - base  # Inherit from base experiment
  - _self_

model:
  use_selection_head: true
  
  # Enhanced visualization for hypersteer
  cross_attn_heatmap_visualization:
    log_heatmap_freq: 5  # More frequent logging
    pdf_visualization: true
    png_visualization: true 

wandb:
  project: "hypersteer"